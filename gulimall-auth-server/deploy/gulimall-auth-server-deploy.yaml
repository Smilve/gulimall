apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gulimall-auth-server
  name: gulimall-auth-server
  namespace: gulimall
spec:
  replicas: 1
  selector:
    matchLabels:
      name: gulimall-auth-server
  template:
    metadata:
      labels:
        name: gulimall-auth-server
    spec:
      containers:
        - name: gulimall-auth-server
          image: $REGISTRY/$DOCKERHUB_NAMESPACE/$PROJECT_NAME:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          resources:
            limits:
              cpu: 300m
              memory: 600Mi
            requests:
              cpu: 100m
              memory: 100Mi
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: gulimall-auth-server
  name: gulimall-auth-server
  namespace: gulimall
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080          # pod端口
      targetPort: 8080    # 容器端口
      nodePort: 31001     # 服务器对外访问的端口，nodePort暴露的端口只能是在30000-32767
  selector:
    name: gulimall-auth-server
  type: NodePort
  sessionAffinity: None

